/**
 * Блок «чего в вашей паре нет».
 *
 * Самый честный и самый редкий блок разбора. Все продают «что у вас есть» —
 * никто не продаёт «на что не надо тратить десять лет».
 *
 * Механика. Канал состоит из двух ворот. Если ни у одного из двоих нет ни той,
 * ни другой половины, темы в паре нет как материала: она не появится ни от
 * работы над отношениями, ни от времени, ни от желания. Если есть только одна
 * половина — тема звучит, но замкнуть её вдвоём нельзя, ответ придёт только
 * извне.
 *
 * ОТКУДА ГЛАВНАЯ ОПАСНОСТЬ ЭТОГО БЛОКА. Отсутствующих каналов у любой пары
 * большинство: их всего 36, а закрывается обычно меньше десятка. Подать это
 * без калибровки — значит напугать человека списком из двадцати семи пунктов
 * «чего у вас нет». Поэтому `norm` обязателен и идёт раньше самого списка.
 *
 * Тексты самих каналов не дублируются: берутся из `CHANNEL_PAIR.appears` —
 * там уже описано, что канал даёт, и для отсутствующей темы этого достаточно.
 */

export const ABSENT_FRAME = {
  intro:
    "Дальше — то, о чём обычно молчат. Разборы совместимости рассказывают, что у пары есть. Но у любых двоих есть и обратное: темы, которых между ними нет и не будет. Знать про них важнее, чем кажется: именно на них уходят годы, потраченные на «мы просто мало старались».",

  norm:
    "Сразу про масштаб, чтобы список ниже не напугал. Тем всего тридцать шесть, и у любой пары закрывается меньше десятка — так устроены все, а не вы. Отсутствующая тема не означает, что с вами что-то не так. Она означает ровно одно: этой краски в вашей палитре нет, и рисовать ею не получится, сколько ни пробуй.",

  water:
    "Разница между «пока не получается» и «не получится» здесь принципиальная. Воду можно нагреть до пара — это одно вещество в разных состояниях. Бензином вода не станет никогда: у неё другой состав. То, что ниже, — про состав, а не про состояние.",

  noneTitle: "Этих тем в паре нет вовсе",
  none:
    "Ни у одного из вас нет ни одной из двух половин. Это значит, что тема между вами просто не звучит — не приглушённо, а никак. Обычно такие вещи и не ощущаются потерей: по тому, чего никогда не пробовал, не скучают. Заметны они по-другому — как область, в которой вы оба одинаково беспомощны и оба этого не замечаете.",

  halfTitle: "Эти темы звучат, но замкнуть их вдвоём нельзя",
  half:
    "Здесь тоньше и болезненнее. Одна половина есть, вторая отсутствует у обоих — значит запрос звучит постоянно, а ответа внутри пары нет. Это то место, где один годами ждёт от второго того, чего второму просто нечем дать. Не «не хочет» и не «не любит» — нечем.",

  halfAction:
    "Что с этим делать. Такая тема закрывается только снаружи: друг, коллега, дело, среда, где эта половина есть у других. Это не измена и не обход отношений — это нормальное устройство человека, которому нужны не только вы двое. Пара, которая позволяет каждому добирать недостающее вовне, живёт дольше той, что пытается быть друг для друга всем.",

  closing:
    "И главное, ради чего этот блок вообще написан. Отделив «можно развить» от «неоткуда взять», ты перестаёшь воевать с тем, что не воюется. Силы, которые уходили на переделку невозможного, освобождаются — обычно их оказывается неожиданно много.",
} as const;
