import styles from "./result.module.css";

/**
 * Строка с продавцом — формальное требование платёжного сервиса.
 *
 * На контентных страницах она приходит из общего подвала, а у страниц
 * результата подвала нет: у них своя вёрстка. При этом именно на них стоит
 * кнопка оплаты, то есть назвать продавца там нужнее всего.
 *
 * Оформления никакого: тот же размер и цвет, что у остальных служебных
 * подписей. Это юридическая формальность, а не часть разбора.
 */
export function SellerLine() {
  return <p className={styles.sellerLine}>Соловьёв Самир Викторович · ИНН 253605749197</p>;
}
